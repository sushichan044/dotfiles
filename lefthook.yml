# lefthook.yml

pre-commit:
  parallel: true
  jobs:
    - name: autofix TypeScript
      group:
        jobs:
          - run: vp check --fix {staged_files}
            glob: "*.{js,cjs,mjs,ts,cts,mts,jsx,tsx}"
            stage_fixed: true
